import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const styles = StyleSheet.create({
    textFont: {
        fontSize: 48,
    },
})

const SignupScreen = () => (
    <View>
        <Text style={styles.textFont}>SignupScreen</Text>
    </View>
)

export default SignupScreen
